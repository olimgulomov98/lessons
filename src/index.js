import React from 'react';
import ReactDOM from 'react-dom/client';
import './index'
import SwiperCarousel from './Swiper/SwiperCarousel.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SwiperCarousel />

  </React.StrictMode>
);

